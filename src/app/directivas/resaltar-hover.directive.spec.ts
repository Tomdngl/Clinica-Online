import { ResaltarHoverDirective } from './resaltar-hover.directive';

describe('ResaltarHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarHoverDirective();
    expect(directive).toBeTruthy();
  });
});
