import { CartasPacientesAdminDirective } from './cartas-pacientes-admin.directive';

describe('CartasPacientesAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new CartasPacientesAdminDirective();
    expect(directive).toBeTruthy();
  });
});
