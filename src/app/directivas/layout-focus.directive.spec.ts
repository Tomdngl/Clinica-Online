import { LayoutFocusDirective } from './layout-focus.directive';

describe('LayoutFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutFocusDirective();
    expect(directive).toBeTruthy();
  });
});
