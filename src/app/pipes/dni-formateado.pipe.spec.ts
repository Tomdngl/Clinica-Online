import { DniFormateadoPipe } from './dni-formateado.pipe';

describe('DniFormateadoPipe', () => {
  it('create an instance', () => {
    const pipe = new DniFormateadoPipe();
    expect(pipe).toBeTruthy();
  });
});
