import { DiaFormateadoPipe } from './dia-formateado.pipe';

describe('DiaFormateadoPipe', () => {
  it('create an instance', () => {
    const pipe = new DiaFormateadoPipe();
    expect(pipe).toBeTruthy();
  });
});
